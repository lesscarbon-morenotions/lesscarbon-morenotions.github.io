# DISABLED — this site is now Astro on GitHub Pages.
# Deploy is handled by deploy-pages.yml (Astro build → Pages artifact).
# Kept as a no-op so an accidental re-enable of the old Jekyll sample does not run.
name: (disabled) Jekyll GitHub Pages

on:
  workflow_dispatch:
    # Manual only; does nothing useful. Prefer "Deploy to GitHub Pages".

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Jekyll workflow disabled
        run: |
          echo "This repository deploys with Astro via deploy-pages.yml."
          echo "Jekyll is no longer used."
          exit 0
